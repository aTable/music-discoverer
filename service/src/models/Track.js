class Track {
  constructor(title, length, startTime, endTime) {
    this.title = title;
    this.length = length;
    this.startTime = startTime;
    this.endTime = endTime
  }
}

module.exports = {
  Track
};
